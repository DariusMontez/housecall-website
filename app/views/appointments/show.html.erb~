<p id="notice"><%= notice %></p>

<h1>Appointment <%= @appointment.id %></h1>

<h2>Data Entries</h2>

<table class="table">
  <thead>
    <tr>
    	<td>Classification</td>
    	<td>Value</td>
    	<td>Unit</td>
    	<td>Timestamp</td>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @appointment.data_entries.each do |data_entry| %>
      <tr data-href="<%= appointment_path(@appointment) %>">
      	<td><%= data_entry.classification %></td>
      	<td><%= data_entry.value %></td>
      	<td><%= data_entry.unit %></td>
      	<td><%= data_entry.created_at %></td>
        <td><%= link_to 'Show', data_entry %></td>
        <td><%= link_to 'Edit', edit_data_entry_path(data_entry) %></td>
        <td><%= link_to 'Destroy', data_entry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_appointment_path(@appointment) %> |
<%= link_to 'Back', appointments_path %>
